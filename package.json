{
  "name": "postman-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/icon.icns --prune=true --out=release-builds"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.5",
    "body-parser": "^1.20.2",
    "electron": "^28.1.4",
    "electron-is-dev": "^3.0.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}
